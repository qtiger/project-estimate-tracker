{csvHeader filename="PETCube.csv"}
{foreach from=$cube item=row}
{foreach from=$row item=entry}"{$entry}",{/foreach}
{* needs an empty line below to force newline otherwise csv file doesn't load properly *}

{/foreach}